# AI Skill: PDF to Word Converter（PDF 转 Word 技能）

## 技能概述

本 AI Skill 的核心能力是将 **PPT 导出的 PDF 文件**智能转换为结构化的 **Word 文档（.docx）**，具备以下特点：

| 能力 | 说明 |
|------|------|
| 📸 页面图像保留 | 将 PDF 每一页渲染为高清图片并嵌入 Word |
| 🤖 AI 内容总结 | 调用 GPT-4o-mini 对全文进行结构化摘要，置于文档首页 |
| 📝 文字提取 | 提取每页文字内容，附于图片下方 |
| 📦 即开即用 | 纯浏览器运行，无需安装任何软件 |

---

## 使用方法

### 快速开始

1. 用浏览器打开 `pdf-to-word-converter.html`
2. 拖拽或点击上传 PDF 文件
3. （可选）填入 OpenAI API Key 以启用 AI 摘要
4. 选择渲染质量和导出选项
5. 点击"开始转换"
6. 转换完成后点击"下载 Word 文档"

### AI 摘要 Prompt 设计

本技能使用以下系统级 Prompt 结构生成内容摘要：

```
角色: 专业内容分析师
任务结构:
  1. 📌 主题概览  — 文档核心主题和目的
  2. 🔑 关键要点  — 最重要观点（条目列出）
  3. 📊 重要数据  — 关键数据、统计、结论
  4. 💡 建议行动项 — 可行的后续步骤（如有）
  5. 📝 总体评价  — 文档结构与内容质量评估
```

---

## 技术架构

```
PDF 文件 (用户输入)
    │
    ▼
[PDF.js]  ──────────────────────────────────────────────────
    │  ① 逐页渲染为 Canvas 图像（JPEG，可调倍率 1.5x–2.5x）
    │  ② 提取每页文字内容（TextContent API）
    ▼
[OpenAI GPT-4o-mini]  ──────────────────────────────────────
    │  ③ 汇总所有页面文字
    │  ④ 生成结构化中/英文摘要
    ▼
[docx.js v8]  ──────────────────────────────────────────────
    │  ⑤ 构建 Word 文档结构:
    │      · 封面标题
    │      · AI 摘要章节
    │      · 每页: 标题 + 图片 + 提取文字
    ▼
.docx 文件 (用户下载)
```

### 依赖库

| 库 | 版本 | 用途 |
|----|------|------|
| [PDF.js](https://mozilla.github.io/pdf.js/) | 3.11.174 | PDF 解析与渲染 |
| [docx](https://docx.js.org/) | 8.5.0 | Word 文档生成 |
| OpenAI API | GPT-4o-mini | 内容智能总结 |

---

## AI 调用规范

### 模型选择策略

| 场景 | 推荐模型 | 原因 |
|------|----------|------|
| 默认总结 | `gpt-4o-mini` | 成本低、速度快、质量好 |
| 高质量报告 | `gpt-4o` | 更深入的分析 |
| 超长文档 | `gpt-4o-mini` + 分块 | 绕过 token 限制 |

### Token 估算

- 输入截断上限：**16,000 字符**（约 12,000 tokens）
- 输出上限：**2,500 tokens**
- 建议每次调用成本：约 **$0.001–$0.003**（gpt-4o-mini）

---

## 输出文档结构

```
📄 [文档标题]
────────────────────────────────
📊 AI 内容摘要
   1. 主题概览 ................ [摘要文字]
   2. 关键要点 ................ [条目列表]
   3. 重要数据/结论 ........... [数据摘录]
   4. 主要建议/行动项 ......... [行动建议]
   5. 总体评价 ................ [质量评估]

════════════════════ 分页 ════════════════════

📄 PDF 页面内容
   ┌─ 第 1 页 ─────────────────────────────┐
   │  [页面截图]                             │
   │  📝 提取文字：...                       │
   └───────────────────────────────────────┘
   ┌─ 第 2 页 ─────────────────────────────┐
   │  ...                                   │
   └───────────────────────────────────────┘
```

---

## 高级使用技巧

### 1. 批量处理（浏览器脚本扩展）

如需批量处理多个 PDF，可在浏览器控制台调用核心函数：

```javascript
// 直接调用内部转换函数
const result = await buildDocx({ fileName, pages, summary, ... });
```

### 2. 自定义 AI Prompt

修改 `generateSummary()` 函数中的 `userPrompt` 变量，可定制摘要风格：

```javascript
// 示例：改为会议纪要格式
const userPrompt = `请将以下 PPT 内容整理为会议纪要格式，包括：
- 会议主题
- 讨论要点
- 决议事项
- 待办行动`;
```

### 3. 渲染质量与文件大小权衡

| 渲染倍率 | 图像质量 | 单页大小 | 适用场景 |
|---------|---------|---------|---------|
| 1.5x | 标准清晰 | ~100 KB | 快速预览 |
| 2.0x | 高清 | ~200 KB | 日常使用（推荐） |
| 2.5x | 超清 | ~400 KB | 正式报告存档 |

---

## 隐私与安全

- 🔒 **PDF 文件本地处理**：PDF 解析和页面渲染全部在用户浏览器本地完成，文件内容不上传到任何服务器。
- 🔑 **API Key 本地存储**：OpenAI API Key 仅存在于当前页面内存中，页面关闭即清除。
- 📤 **文字内容发送 OpenAI**：仅当用户填写 API Key 并勾选"AI 总结"时，提取的文字内容会发送至 OpenAI API（遵循 OpenAI 数据使用政策）。

---

## 常见问题

**Q: PDF 页面图片模糊怎么办？**  
A: 在渲染质量中选择"高清（2.0x）"或"超清（2.5x）"。

**Q: 文字提取为空？**  
A: 扫描版 PDF 无法提取文字（仅含图像），截图仍会被完整保留。可使用 AI 视觉模型（GPT-4o vision）识别文字。

**Q: AI 摘要不准确？**  
A: 文字提取不完整时（如 PPT 使用大量文字框叠加），可降低摘要期望值，或手动补充关键信息。

**Q: 生成的 Word 文件很大？**  
A: 降低渲染倍率至 1.5x，并取消勾选"超清"选项。页数越多文件越大，属正常现象。

---

## 版本历史

| 版本 | 日期 | 更新内容 |
|------|------|---------|
| v1.0 | 2026-04-22 | 初始版本：PDF.js 渲染 + docx.js 生成 + GPT-4o-mini 摘要 |

---

*本技能属于 AI-Skills 项目的一部分。*
